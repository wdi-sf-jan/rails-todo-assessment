<h3>
  Todo Lists
</h3>

<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list %> / <%= link_to "Edit", edit_list_path(list) %> / <%= link_to "Delete", list_path(list), method: "DELETE", data: {confirm: "Are you sure?"} %></li>
  <% end %>
</ul>

<p>
  <%= link_to "Add a list", new_list_path %>
</p>